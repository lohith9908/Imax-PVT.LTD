git pull origin dev
remote to local

git push origin dev
local to remote

git status

git branch 

 to create a new branch

git branch dev

git checkout dev

git push origin dev


git add -A

git commit -m "commit message"

git push origin dev
